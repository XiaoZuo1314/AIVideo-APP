my-app/
├── app/                    # 路由目录（Expo Router）
│   ├── (tabs)/             # 底部标签导航组
│   │   ├── _layout.tsx     # Tab 布局
│   │   ├── index.tsx       # 首页
│   │   └── profile.tsx     # 个人中心
│   ├── _layout.tsx         # 根布局（全局 provider、导航容器）
│   ├── modal.tsx           # 模态路由
│   └── post/[id].tsx       # 动态路由示例
│
├── src/                    # 业务代码聚集地
│   ├── components/         # 可复用 UI 组件
│   │   ├── ui/             # 原子组件（Button, Card, Input）
│   │   └── business/       # 业务组件（UserCard, PostItem）
│   ├── features/           # 按功能拆分的模块（推荐）
│   │   ├── auth/
│   │   │   ├── api.ts
│   │   │   ├── hooks.ts
│   │   │   └── components/
│   │   ├── post/
│   │   └── profile/
│   ├── hooks/              # 全局复用 hooks
│   ├── stores/             # 状态管理（zustand, jotai 等）
│   ├── utils/              # 纯函数工具
│   ├── constants/          # 静态常量
│   ├── theme/              # 主题、颜色、字体、间距
│   └── types/              # TypeScript 全局类型声明
│
├── assets/                 # 静态资源（图片、字体、Lottie 动画）
├── config/                 # 第三方服务配置（如 Firebase, Sentry）
├── eas.json                # EAS Build 配置（iOS 构建用）
├── app.json                # Expo 核心配置（包名、图标、权限）
├── tsconfig.json
├── package.json
└── .env / .env.production  # 环境变量（通过 expo-constants 使用）